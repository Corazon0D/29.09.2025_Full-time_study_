# Срезы (s, s, s)
# срезы [start : stop -1 : step]

string = input('Введите любую строку: ')
length = len(string) # число элементов итерируемого объекта

print(string[::-1]) # Разворот
# Вентилятор -> лят